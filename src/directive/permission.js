import { getPermission } from '../libs/util'
export default {
    inserted(el, binding, vnode) {
        // console.log(binding)
        const { value } = binding
        const access = {}
        // if (value.type) {
        // } else {
        //     throw new Error('缺少参数')
        // }
    }
}
